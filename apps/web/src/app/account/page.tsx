'use client';
import React,{useState} from 'react';import Link from 'next/link';import {hub,useHub} from '../../lib/hub';import {AppFrame,LoadState} from '../../components/HubFrames';
export default function Account(){
 const state=useHub('/session'),[email,setEmail]=useState(''),[code,setCode]=useState(''),[sent,setSent]=useState(false),[busy,setBusy]=useState(false),[message,setMessage]=useState(''),[error,setError]=useState('');
 async function act(path:string,body:unknown){if(busy)return;setBusy(true);setMessage('');setError('');try{await hub(path,body);if(path==='/auth/email'){setSent(true);setCode('');setMessage('Check your email for the verification code.');}else{setSent(false);setCode('');setMessage(path==='/auth/logout'?'You are signed out.':'Your account is connected.');}}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
 return <AppFrame title="My Account"><p className="lead">Your skincare, connected to you.</p><LoadState {...state} retry={state.reload}/>{message&&<p className="notice success" role="status">{message}</p>}{error&&<p className="notice error" role="alert">{error}</p>}
 {!state.loading&&!state.error&&state.data&&(state.data.account?<><section className="panel account-panel"><span className="eyebrow">CONNECTED ACCOUNT</span><h2>Welcome back.</h2><p className="account-email">{state.data.account.email}</p><div className="actions"><Link className="button primary" href="/my-skin">View my profile</Link><button disabled={busy} type="button" className="button" onClick={()=>void act('/auth/logout',{})}>{busy?'Signing out…':'Sign out'}</button></div></section><div className="actions"><Link className="button" href="/membership">Plans & Billing</Link></div><section className="account-tools" aria-label="Account tools"><a href="/api/hub/account/export"><strong>Download my data <span aria-hidden="true">↓</span></strong><span>Export the information saved to your account.</span></a><Link href="/orders"><strong>Retailer purchase help <span aria-hidden="true">→</span></strong><span>Find where to ask about payments, delivery and returns.</span></Link></section></>:<form className="panel stack account-panel" onSubmit={e=>{e.preventDefault();void act(sent?'/auth/verify':'/auth/email',sent?{email:email.trim(),code}:{email:email.trim()});}}><span className="eyebrow">{sent?'STEP 2 OF 2 · VERIFY':'STEP 1 OF 2 · EMAIL'}</span><h2>{sent?'Check your inbox.':'Keep your skincare connected.'}</h2><p>{sent?'Enter the verification code sent to your email address.':'Sign in with a code sent to your email. Your current browser’s profile can be linked to your account.'}</p>{!state.data.auth_configured&&<p className="notice">Email sign-in is awaiting service configuration. Your sample profile can still be saved in this browser’s portal session.</p>}<label className="field">Email address<input type="email" required autoComplete="email" disabled={busy||sent} value={email} onChange={e=>setEmail(e.target.value)}/></label>{sent&&<label className="field">Verification code<input className="verification-code" required inputMode="numeric" autoComplete="one-time-code" pattern="[0-9]{6,8}" maxLength={8} disabled={busy} value={code} onChange={e=>setCode(e.target.value)} aria-describedby="code-help"/><span className="muted" id="code-help">Enter the 6–8 digit code from your email.</span></label>}<button className="button primary" disabled={busy||!state.data.auth_configured}>{busy?'Please wait…':sent?'Verify and sign in':'Email me a code'}</button>{sent&&<button type="button" className="text-button" disabled={busy} onClick={()=>{setSent(false);setCode('');setMessage('');setError('');}}>Use a different email or request a new code</button>}</form>)}</AppFrame>;
}
